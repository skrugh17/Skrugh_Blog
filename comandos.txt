pwd: muestra de carpeta de usuario 
cd /: change directory 

ls : listar archivos se la carpeta actual
ls -l:  Muestra lista de archivos 
ls -al: Muestra lista de archivos más archivos ocultos
ls -a: mustra los archivos por no en forma de lista
mkdir +"nombre_carpeta": Crear carpeta 
touch +"nombre_archivo": crea Archivo nuevo
cat +"archivo" : muestra contenido de archivo
history: muestra el historial de comandos 
! + número: Ejecuta comando con el número que nos muestra el history ej, !72
rm +"archivo": elimina archivo 
clear ó ctrl + l: Limpiar consola  


git init: se crea un repositorio y sale una carpeta .git Se crea el master
staging: es la memoria ram donde guarda la carpeta.git
git add  +"archivo" = agrega el archivo al staging
<<<<<<< HEAD
git checkout: el archivo va al repositorio (master)  
git commit-m" ": Traer lo cambios realizados en archivos del master
git commit -am "mensaje" : agrega los cambios despues de que ya envias cambios con add 
Ramas (branch) 
git branch: Ver las ramas que estamos usando.
=======
commit-m: el archivo va al repositorio (master)  
checkout: Traer lo cambios realizados en archivos del master
git commit -am "mensaje" : agrega los cambios despues de que ya envias cambios con add 
Ramas (brench) 

>>>>>>> cabecera
Master: Guarda las diferentes verciones, es el lugar donde están guardados los archivos "commit"
Experimentos/development: Sirve para crear verciones no oficiales
bugfixing/hotfixing: Es el campo donde arreglamos los bugs del programa 
merge: enviar el archivo de experimentos al master -tener cuidado con el proceso porque puede generar "conflictos"
git push origin "nombre_rama": subir las ramas a github
git pull origin master: traer cambios del origen al master


git log: historial de cambios en el archivo
git reset: volver a la version anterior 

github:
git remote add origin https://github.com/skrugh17/Skrugh_Blog.git: 
git remote
git remote -v

LLaves publicas y privadas:
$ ssh-keygen -t rsa -b 4096 -C "skrugh17@gmail.com": Crear llaves


$ eval $(ssh-agent -s) :  revisar que las llaver esten funcionando

Tags
git tag -a 'nombre del tag' -m "mensaje" xxxxx (el hash que quiero taggear)
git tag: para ver los taggs existentes
git show-ref --tags: mostrat el tag y su información
git pull origin master: traer cambios generados en github
git push origin --tags: Subir el tag a github
git tag -d 'nombre del tag para borrar': Borrar etiquetas
git push origin :refs/tags/pepe: borrar la etiqueta en github

Traer proyectos:
git clone "url del proyecto" 














